# Set this in Vercel: Project -> Settings -> Environment Variables.
# ClickUp -> avatar (bottom left) -> Settings -> Apps -> API Token -> Generate.
# Never commit the real token. It stays server-side and is never sent to the browser.
CLICKUP_API_TOKEN=pk_xxxxxxxx_XXXXXXXXXXXXXXXXXXXXXXXXXXXX
